# DSH 桌面浏览器（外壳 + 插件）

让 DSH 侧边栏那一格成为一个**真正的浏览器**：真渲染、能登录、什么网站都能开，并且 Agent 能全功能操作它。本文件是项目术语表（glossary），只定义词，不含实现细节。

## Language

**shell（外壳）**:
本项目自建的薄 Electron 应用：它用一个窗口装 DSH 的 Web 界面，并在窗口内额外持有一块**原生视图**。它不修改 DSH 本体。
_Avoid_: 桌面端、客户端、包装器

**native view（原生视图）**:
由外壳自己渲染的一块真正的浏览器视图（Electron 的 `WebContentsView`）。**它就是浏览器本身**——真 DOM、真登录态、可被程序驱动。
_Avoid_: 内嵌浏览器、iframe、预览

**screencast（画面流）** 【已废弃】:
把浏览器页面当作连续截图搬到面板里显示的做法（MJPEG / `Page.startScreencast`）。**本项目明确废弃**：它是"网页里放不下程序窗口"这一限制的妥协产物，一旦界面由外壳自己渲染就不需要了。
_Avoid_: 一切把"画面"当作浏览器视图的说法

**adopt（领养）**:
插件接管**一块已经存在的**原生视图，而不是自己启动浏览器。这是本项目相对既有实现最根本的动作差异。
_Avoid_: attach、连接、绑定（太泛）

**view contract（视图契约）**:
外壳与插件之间那条**很窄**的接口：创建/导航/定位/销毁原生视图，以及把这块视图的可编程端点（CDP）与它的**身份**交给插件。除这条契约外，双方互不知道对方内部。
_Avoid_: API、协议、桥

**carrier（载体）**:
客户端与服务端之间承载 RPC 的物理通道。两种：浏览器里是 HTTP + WebSocket；外壳里是外壳自己的通道（**不开任何监听端口**）。插件代码对两者无感。
_Avoid_: 传输层、transport（口语可用，术语用"载体"）

**panel（面板）**:
插件在 DSH 右栏那一格里的**网页部分**。它不渲染网页内容，只负责把自己占据的矩形与可见性上报给外壳，并处理生命周期（被切走、折叠、浮动）。
_Avoid_: 视图、窗口、tab 内容

**plugin（插件）**:
DSH 的最小能力单元：一个导出 `apply(ctx)` 的 TypeScript 模块，通过 `ctx` 注册能力。
_Avoid_: 扩展、模块、addon

**tool（工具）**:
面向模型的能力，Agent 按声明好的 schema 调用；用 `defineTool` 定义、`ctx.tools.register` 注册。
_Avoid_: 函数、action、命令

**browser-use（本项目语境）**:
让 Agent 操控浏览器网页的能力（导航、点击、输入、读取、截图等）。
_Avoid_: 浏览器自动化、爬虫、RPA

**snapshot（页面快照）**:
把当前页面可交互元素序列化成带数字索引（`[1]` `[2]`…）的文本列表，供模型引用元素。**紧凑**：数据区另用读取正文的能力按需取，不塞进快照。
_Avoid_: DOM 树、HTML 原文、完整可访问性树

**element ref（元素引用）**:
模型在动作里定位元素的方式——引用快照里的数字索引（如 `ref=3`），而非手写 CSS/XPath。

**task space（任务空间）**:
一次任务独占的浏览器状态（上下文 + cookie/存储），任务之间互不串扰；新空间可继承默认档案的登录态；关闭即释放。
_Avoid_: 会话、沙箱、隔离区

**persistent profile（持久登录档案）**:
一个持久化的浏览器档案目录，登录态/cookie 存于此、跨外壳重启保留。
_Avoid_: 用户数据、缓存目录

**proxy（代理）**:
浏览器访问网络时走的代理服务器地址；启动时自动探测（显式配置 → 环境变量 → Windows 系统代理），本地回环一律 bypass。

**cursor overlay（光标覆盖层）**:
把 Agent 的动作（点击、移动、滚动、读取）画在页面上的可见提示，让人看得到 Agent 正在干什么；换页后自动重挂。
